# Docker Compose Environment Variables
# Copy this file to .env and fill in the values
# Usage: cp .env.docker.example .env

# ===========================================
# Port Configuration (using available ports)
# ===========================================
# Backend API:   http://localhost:8001
# PostgreSQL:    localhost:5435
# Mock Jira API: http://localhost:8443 (with --profile mock)
# Frontend:      http://localhost:3000 (with --profile dev)

# ===========================================
# Quick Start Commands
# ===========================================
# Production (real Jira):
#   docker compose up -d
#
# Development with Mock Jira (recommended for load testing):
#   docker compose --profile mock up -d
#
# Full dev stack (mock Jira + frontend hot reload):
#   docker compose --profile mock --profile dev up -d

# ===========================================
# PostgreSQL Configuration
# ===========================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_DB=capacity_planner

# ===========================================
# Jira Configuration
# ===========================================
# OPTION 1: Use Mock Jira (for development/load testing)
# When using --profile mock, the mock server is available at http://mock-jira:8443 (internal)
# Default is set to mock server - no real credentials needed
JIRA_BASE_URL=http://mock-jira:8443
JIRA_EMAIL=mock@test.com
JIRA_API_TOKEN=mock-token

# OPTION 2: Use Real Jira (for production)
# Uncomment and fill in these values to use real Jira Cloud
# JIRA_BASE_URL=https://sntech.atlassian.net
# JIRA_EMAIL=your.email@example.com
# JIRA_API_TOKEN=your_jira_api_token_here

JIRA_DOMAIN=sntech
# Comma-separated list of Jira project IDs to filter (leave empty for mock to use all)
JIRA_PROJECT_IDS=

# Jira Custom Field Mappings (optional - defaults match mock server)
# JIRA_IT_OWNER_FIELD=customfield_10000
# JIRA_BUSINESS_CHAMPION_FIELD=customfield_10078
# JIRA_WORKSTREAM_FIELD=customfield_10447
# JIRA_INSERVICE_DATE_FIELD=customfield_10121
# JIRA_START_DATE_FIELD=customfield_10015
# JIRA_END_DATE_FIELD=customfield_10685
# JIRA_PAR_FIELD=customfield_10132
# JIRA_HEALTH_STATUS_FIELD=customfield_10451
# JIRA_BUSINESS_VALUE_FIELD=customfield_10200
# JIRA_CAPITAL_EXPENSE_FIELD=customfield_10450
# JIRA_EPIC_ISSUE_TYPE_ID=10000
# JIRA_INITIATIVE_ISSUE_TYPE_ID=10001

# ===========================================
# Mock Jira Configuration (optional)
# ===========================================
# Customize the mock data generation
# MOCK_JIRA_NUM_PROJECTS=1500
# MOCK_JIRA_NUM_INITIATIVES=1500
# MOCK_JIRA_EPICS_PER_PROJECT_MIN=200
# MOCK_JIRA_EPICS_PER_PROJECT_MAX=250
# MOCK_JIRA_TASKS_PER_EPIC_MIN=100
# MOCK_JIRA_TASKS_PER_EPIC_MAX=150
# MOCK_JIRA_NUM_USERS=500
# MOCK_JIRA_DATA_SEED=capacity-planner-mock-2024

# ===========================================
# Tempo Configuration
# ===========================================
TEMPO_API_TOKEN=your_tempo_api_token_here

# ===========================================
# Authentication (optional)
# ===========================================
# Set to true to enforce role checks
AUTH_ENABLED=false

# ===========================================
# Node Environment
# ===========================================
# Use development for mock Jira testing (bypasses auth when AUTH_ENABLED=false)
# Use production for real deployments
NODE_ENV=development
